package com.example.recipes.datasouce.room

import com.example.recipes.data.Recipe

//class RoomUseCase(private val dataBase: RecipeDataBase) {
//
//    suspend fun saveRecipeToRoom(recipe: Recipe) {
//        dataBase.recipesDao().insertRecipes(ConvertersRoom.convertToRecipeEntity(recipe))
//    }
//}